%p#notice= notice

-# in app/views/movies/show.html.haml

%h2 Details about #{@room.room_id}


= link_to 'Edit', edit_room_path(@room)
= button_to 'Delete', room_path(@room), :method => :delete, :confirm => 'Are you sure?'
= link_to 'Back to room list', rooms_path

