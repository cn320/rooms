%h1 Search room

= form_tag search_result_path(@rooms) ,:method=>:get do

  = label :room, :volume,'Volume'
  = text_field :room, 'volume'

  = label :room, :day, 'Day'
  = text_field :room, @day_list

  = label :section, 'Section'
  = select :room, :section, @section

  = submit_tag 'ค้นหา'

= link_to 'Back', search_result_path
