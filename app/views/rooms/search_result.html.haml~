%h1 Show search room

%p แสดงผล time : #{@time_select} , day : #{@day_select}

%table#rooms
  %thead
    %tr
      %th roomname
      %th amount
      %th more info
  %tbody
    - @free_rooms.each do |room|
      %tr
        %td= room.roomname
        %td= room.amount
        - if @admin == nil
          %td= link_to "ดูสถานะห้อง #{room.roomname}", room_path(room)
        - else
          %td= link_to "ดูสถานะห้อง #{room.roomname}", staff_path(room)
        

= link_to 'Back', rooms_path
