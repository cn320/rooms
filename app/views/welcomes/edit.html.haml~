%h3 กรุณาทำเครื่องหมาย หากต้องการเลือกรูป #{#welcome.img_filr_name} แสดงผล

= form_tag welcome_path(@welcome), :method => :put do

  = "คุณต้องการแสดงผลรูปนี้"
  = hidden_field_tag "welcome[choose]",0
  = check_box_tag "welcome[choose]",1, @welcome.choose == 1 ? true:false

  = submit_tag "ยืนยัน"

%br
= link_to 'Back', welcomes_path
