%h1 New reserf

%h2 Reserve #{@room.room_id}


= form_tag reserves_path do

  = label :reserve, :name, 'Name'
  = text_field :reserve, 'name'

  = label :reserve, :room_id, 'Room_id'
  = text_field :reserve, 'room_id'

  = submit_tag 'ยืนยันการจองห้อง'
= link_to 'Back', reserves_path
